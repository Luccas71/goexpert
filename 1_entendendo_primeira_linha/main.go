package main

/*
	PACKAGE
	=> todo package leva o nome do diretorio onde está o arq. Exceto o main

	=> todos arq no mesmo diretorio deve ter o mesmo package

	=> todos arq dentro de um mesmo package compartilham var e funs
*/

func main() {
	println(a)
}
